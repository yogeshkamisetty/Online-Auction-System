services:
  - type: web
    name: golden-hammer-api
    runtime: node
    plan: free
    buildCommand: npm install && npx prisma generate && npx prisma migrate deploy
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: JWT_EXPIRES_IN
        value: 365d
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: CORS_ORIGIN
        sync: false
      # Public URL of the Vercel frontend — used in transactional email links.
      - key: CLIENT_URL
        sync: false
      # Resend API key — without it, emails are simulated to the server log.
      - key: RESEND_API_KEY
        sync: false
